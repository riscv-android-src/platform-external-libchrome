# This file will be used by emerge in libchrome-XXXX.ebuild to determine the
# order of applying patches.

# Remove glib dependency.
# TODO(hidehiko): Fix the config in AOSP libchrome.
libchrome-Remove-glib-dependency.patch

# Refer to crbug/1093642 on upstream or abandon these two patches.
# Add WaitForServiceToBeAvailable back for MockObjectProxy
WaitForServiceToBeAvailable.patch
# Add MockObjectManager, which is removed upstream.
add-mock-object-manager.patch

# Remove after r802050
libchrome-fix-integer-overflow-if-microseconds-is-IN.patch

# Solve name conflict with abseil in absl package, when a program in Chromium
# OS depends on both abseil and libchrome.
absl-thread-annotation-conflict.patch

# Diagnostic code for upsteram to investigate crbug/1001171.
# We don't need.
revert-crbug-1001171.patch

# Verify MessageLoop migration, and remove it after uprev.
backward-compatibility-message-loop.patch

# Remove after uprev to r780000
py3.patch

# Remove after uprev to r784327
forward-compatibility-file-util.patch

# Remove after uprev to r784774
forward-compatibility-current-thread.patch

# Remove after uprev to r798820
forward-compatibility-string-utils.patch

# Remove after uprev to r806478
forward-compatibility-file-lock.patch

# Remove after uprev to r806816
android-allow-null-exception-reporter.patch

# Remove after uprev to r807076
0001-Fix-pending_broker_clients-handling.patch

# Remove after uprev to r798795
mojo-core-io-runner-subclass.patch

# Remove after uprev to r820349
forward-compatibility-logging.patch
