From 13807fc6598101817e6cff3ea14676acc956abeb Mon Sep 17 00:00:00 2001
From: hscham <hscham@chromium.org>
Date: Wed, 2 Dec 2020 16:38:09 +0900
Subject: [PATCH] Remove default mode from base::File::Lock

Change-Id: Ibb24bb1baebeb1a40c32f5190610855c60175962
---
 base/files/file.h | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/base/files/file.h b/base/files/file.h
index a5ba395edb..5ede832ec5 100644
--- a/base/files/file.h
+++ b/base/files/file.h
@@ -299,7 +299,7 @@ class BASE_EXPORT File {
   //  * Within a process, locking the same file (by the same or new handle)
   //    will succeed. The new lock replaces the old lock.
   //  * Closing any descriptor on a given file releases the lock.
-  Error Lock(LockMode mode = LockMode::kExclusive);
+  Error Lock(LockMode mode);
 
   // Unlock a file previously locked.
   Error Unlock();
-- 
2.29.2.454.gaff20da3a2-goog

